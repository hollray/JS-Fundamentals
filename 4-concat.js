//JavaScript script that concatenates two strings

//define variables to hold the two strings from command line arguments
const str1 = process.argv[2];
const str2 = process.argv[3];

//print the concatenated result
console.log(`${str1} is ${str2}`);

